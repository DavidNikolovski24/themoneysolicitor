import { IEnteredData } from "../FormComponent";

interface Props {
  setFormControl: any;
  enteredData: IEnteredData;
  setEnteredData: any;
}
const Form3 = ({}: Props) => {
  return <div id="form-3">3</div>;
};

export default Form3;
