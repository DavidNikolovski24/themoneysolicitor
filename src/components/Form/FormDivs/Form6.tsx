import { IEnteredData } from "../FormComponent";

interface Props {
  setFormControl: any;
  enteredData: IEnteredData;
  setEnteredData: any;
}

const Form6 = ({}: Props) => {
  return <div id="form-6"></div>;
};

export default Form6;
