import { IEnteredData } from "../FormComponent";

interface Props {
  setFormControl: any;
  enteredData: IEnteredData;
  setEnteredData: any;
}

const Form8 = ({}: Props) => {
  return <div id="form-8"></div>;
};

export default Form8;
