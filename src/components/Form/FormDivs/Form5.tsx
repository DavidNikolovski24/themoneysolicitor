import { IEnteredData } from "../FormComponent";

interface Props {
  setFormControl: any;
  enteredData: IEnteredData;
  setEnteredData: any;
}

const Form5 = ({}: Props) => {
  return <div id="form-5"></div>;
};

export default Form5;
